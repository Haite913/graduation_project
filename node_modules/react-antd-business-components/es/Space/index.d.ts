/// <reference types="react" />
import { SpaceProps } from 'antd';
/**
 * 基于 antd 适配为 sec 的格式
 */
declare const RSpace: {
    (props: SpaceProps): JSX.Element;
    defaultProps?: Record<string, any>;
    displayName: string;
};
export default RSpace;
