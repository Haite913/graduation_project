function getStringLength(str = "") {
  const match = str.match(/[\u4e00-\u9fa5]/g);
  let chineseHalfength = 0;
  if (match) {
    chineseHalfength = match.length;
  }
  return str.length + chineseHalfength;
}
export {
  getStringLength as default
};
