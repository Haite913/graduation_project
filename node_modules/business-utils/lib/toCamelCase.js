"use strict";
Object.defineProperties(exports, { __esModule: { value: true }, [Symbol.toStringTag]: { value: "Module" } });
const camelcase = require("camelcase");
const isPlainObject = require("./isPlainObject");
const _interopDefaultLegacy = (e) => e && typeof e === "object" && "default" in e ? e : { default: e };
const camelcase__default = /* @__PURE__ */ _interopDefaultLegacy(camelcase);
const isPlainObject__default = /* @__PURE__ */ _interopDefaultLegacy(isPlainObject);
function toCamelCase(obj) {
  if (!isPlainObject__default.default(obj) && !Array.isArray(obj)) {
    return obj;
  }
  if (Array.isArray(obj)) {
    const reArr = [];
    obj.forEach((arr, index) => {
      reArr[index] = toCamelCase(arr);
    });
    return reArr;
  }
  const reObj = {};
  for (const oldKey in obj) {
    if (isPlainObject__default.default(obj[oldKey])) {
      obj[oldKey] = toCamelCase(obj[oldKey]);
    } else if (Array.isArray(obj[oldKey])) {
      obj[oldKey].forEach((arrValue, index) => {
        obj[oldKey][index] = toCamelCase(arrValue);
      });
    }
    const newKey = camelcase__default.default(oldKey);
    reObj[newKey] = obj[oldKey];
  }
  return reObj;
}
exports.default = toCamelCase;
